pagehelper插件

```java
    <plugins>
        <plugin interceptor="com.github.pagehelper.PageInterceptor"></plugin>
    </plugins>
        //核心配置文件里标签的位置有要求
      //  The content of element type "configuration" must match "(properties?,settings?,typeAliases?,typeHandlers?,objectFactory?,objectWrapperFactory?,reflectorFactory?,plugins?,environments?,databaseIdProvider?,mappers?)".
        
```





![image-20250719035154384](C:\Users\zzy13\AppData\Roaming\Typora\typora-user-images\image-20250719035154384.png)





```java
        InputStream inputStream = Resources.getResourceAsStream("MybatisConfig.xml");
        SqlSessionFactory sqlSessionFactory = new SqlSessionFactoryBuilder().build(inputStream);
        SqlSession session = sqlSessionFactory.openSession();
        UserMapperInterface mapper = session.getMapper(UserMapperInterface.class);
        

        PageHelper.startPage(3, 2);


        List<User> users = mapper.selectall();
        for (User user : users) {
            user.information();
        }


        PageInfo<User> pageInfo = new PageInfo<User>(users);
		//要传入返回的结果users确定是哪一页


        System.out.println("当前页:" + pageInfo.getPages());
        System.out.println("总页数:" + pageInfo.getPages());
        System.out.println("总记录数:" + pageInfo.getTotal());
        System.out.println("当前页的记录数:" + pageInfo.getList().size());
```

