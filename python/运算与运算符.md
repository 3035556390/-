```python
a=0.1
b=0.2
c=0.3

if(a+b==c):
    print("相等")
else:
    print("不相等")
#由于精度问题比较两个数时应该比较两个数的差的绝对值是否小于一个足够小的数
#abs(x-0.3)<1e-6
```

```python
#+号可将列表,元组,字符串拼接
l1=[1,2,3]
l2=[4,5,6]
print(l1+l2)

y1=(1,1)
y2=(2,2)
print(y1+y2)

```

```python
print(3/2)   #不同数字类型之间的运算可以混合运算,结果为最宽的类型
print(5.0+1+3j)   #6+3j

print(1|2)   #01 10  按位或
print(1&2)   #按位与
print(1^2)   #按位异或

2**3  #幂运算
3//2  #整除
```

```python
print(abs(-1.2))  #绝对值
print(abs(2+3j))   #模值,二维坐标点到原地的距离  即根号13

print(max(1,2,3))
print(min(1,2,3))
print(sum([1,2,3]))

#十进制转化为2,8,16进制
print(bin(2))	#0b10
print(oct(10))	#0o12
print(hex(20))	#0x14
```

```python

#in 成员运算符  not in
5 in [4,5,6] 	#True
me in "disappointment"  #True
lst=["dog","cat","fox"]
"cat" in lst   #True
```

```python
x=10
y=10
print(x is y)  #python 是先运算等号右边然后将值随机存储到一个位置,然后变量指向这个位置 所以x is y
a=[1,2]
b=[1,2]
print(a is b)   #a,b两个列表是不同的对象,存储于不同位置,所以a 不是b  ,但a=b值相等
print(a==b)

```

